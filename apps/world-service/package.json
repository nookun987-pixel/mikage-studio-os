{
  "name": "@mikage/world-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts",
    "lint": "biome check src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mikage/config": "workspace:*",
    "@mikage/contracts": "workspace:*",
    "@mikage/database": "workspace:*",
    "@mikage/logging": "workspace:*"
  }
}

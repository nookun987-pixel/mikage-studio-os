{
  "name": "@mikage/orchestration-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts --start",
    "start": "node dist/main.js",
    "lint": "biome check src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mikage/config": "workspace:*",
    "@mikage/contracts": "workspace:*",
    "@mikage/logging": "workspace:*",
    "@mikage/queue": "workspace:*",
    "@mikage/runtime-orchestration-boundary": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.20.5"
  }
}

# Seed Data

Layer 2 JSON seed inputs currently included:

- `000_projects.seed.json`
- `003_world_entities.seed.json`
- `010_prompt_presets.seed.json`

These files are loaded by `scripts/seed.ts` into PostgreSQL via Prisma.

{
  "name": "@mikage/real-queue-backend-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mikage/database": "workspace:*",
    "@mikage/durable-persistence-adapters": "workspace:*",
    "@mikage/structured-logging": "workspace:*",
    "@mikage/studio-job-orchestration": "workspace:*",
    "zod": "^3.24.2"
  }
}
